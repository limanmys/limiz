# Compiled binaries
limiz
limiz.exe
setup.exe
setup/limiz.exe

# Plugin binaries and signatures
plugins/metric/*
plugins/data/*
!plugins/metric/.gitkeep
!plugins/data/.gitkeep

# Build artifacts
build/deb/*.deb
build/deb/limiz_*/

# Private signing key — NEVER commit the private key
keys/plugin-signing.key

# Local metric databases
*.db
*.db-wal
*.db-shm

# Runtime files
*.jsonl
status.txt

# OS / editor
.DS_Store
Thumbs.db
*.swp
*.swo
build/rpm/rpmbuild/SOURCES/limiz-1.0.0.tar.gz
build/rpm/limiz-1.0.0-1.x86_64.rpm
build/rpm/rpmbuild/RPMS/x86_64/limiz-1.0.0-1.x86_64.rpm
